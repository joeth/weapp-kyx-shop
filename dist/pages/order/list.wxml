
<view class="order">
    <wxc-tab class="tabbar" line-color="transparent" active-line-color="{{colors.success}}" default-index="{{activeKey}}" animate="{{true}}">
        <view class="tabbar-nav" slot="tablabel">
            <wxc-tab-label class="tabbar-label {{activeKey == index ? 'active' : ''}}"  wx:for="{{orderTabs}}" wx:for-item="tab" wx:key="{{tab.title}}" tab-index="{{index}}">
                <button class="tabbar-button" type="normal"  bind:tap="onTabClick" data-id="{{tab.id}}">
                    {{tab.title}}
                    <wxc-badge class="tabbar-badge" max="99" value="{{tab.badge}}" wx:if="{{tab.badge}}"/>
                </button>
            </wxc-tab-label>
        </view>
        <view class="tabbar-main">
            <wxc-tab-panel  wx:for="{{orderTabs}}" wx:for-item="tab" wx:key="{{tab.id}}" tab-index="{{index}}">
                <view class="order-list">
                    <block wx:for="{{orderList}}" wx:for-index="oindex" wx:for-item="order" wx:key="oid">
                        
    <view class="cp-item-order is-complete">
        <wxc-flex class="cp-item-order-head" main="between">
            <text class="cp-item-order-code">订单编号：{{order.code}}</text>
            <text class="cp-item-order-status">{{order.statusText}}</text>
        </wxc-flex>
        <view class="cp-item-order-main">
            <view class="order-goods-list">
                <block wx:for="{{2}}" wx:for-index="$ItemOrder$gindex" wx:for-item="$ItemOrder$goods" wx:key="$ItemOrder$gid">
                    
    <wxc-flex class="cp-order-goods">
        <image class="cp-order-goods-cover" src="{{$ItemOrder$order.goods.cover}}"/>
        <wxc-flex class="cp-order-goods-main" dir="top" main="between">
            <view class="cp-order-goods-info">
                <text class="cp-order-goods-row cp-order-goods-title">{{$ItemOrder$order.goods.title}}</text>
                <text class="cp-order-goods-row cp-order-goods-text">{{$ItemOrder$order.goods.type}}</text>
            </view>
            <wxc-flex class="cp-order-goods-count" main="between" cross="center">
                <wxc-price class="cp-order-goods-price" value="{{$ItemOrder$order.goods.price}}"/>
                <text class="cp-order-goods-count">x{{$ItemOrder$order.goods.count}}</text>
            </wxc-flex>
        </wxc-flex>
    </wxc-flex>

                </block>
            </view>
        </view>
        <view class="cp-item-order-foot">
            <wxc-flex class="cp-item-order-info" main="between" dir="right">
                <view>合计（含运费）：<wxc-price class="price" value="{{order.price+order.freight}}"/></view>
            </wxc-flex>
            <wxc-flex class="cp-item-order-tool" main="end" wrap="wrap">
                <view class="button"><button type="warn">取消订单</button></view>
                <view class="button"><button type="base">马上支付</button></view>
                <!-- <view class="button"><button type="base">确认收货</button></view>
                <view class="button"><button type="base">查看订单</button></view>
                <view class="button"><button type="base">发表评价</button></view> -->
            </wxc-flex>
        </view>
    </view>

                    </block>
                </view>
            </wxc-tab-panel>
        </view>
    </wxc-tab>
</view>